# Configuración de URLs
- La variable `VITE_API_BASE_URL` nunca debe llevar barra diagonal (`/`) al final.
- Debe apuntar a: `https://gestorasistencia-worker.camarillocj.workers.dev`.
- La variable `VITE_SITE_URL` debe ser: `https://gestorasistencia2026v2.pages.dev` (sin barra final).